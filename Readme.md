# ⚗️ ColorAlchemy

Десктопное приложение для генерации цветовых палитр, анализа доступности интерфейсов (Accessibility) и ручной калибровки оттенков. Проект ориентирован на создание цветовых схем, соответствующих стандартам WCAG 2.0.

## 📡 Функционал

Приложение решает следующие задачи:

* **Генерация палитр:** 6 алгоритмов подбора (Smart UI, Комплементарный, Триада, Тетрада, Аналоговый, Случайный).
* **Режим Smart UI:** Автоматическое распределение ролей цветов (Фон, Текст, Акцент, Вторичный) на основе яркости базового цвета.
* **Анализ доступности (WCAG 2.0):** Расчет коэффициента контрастности в реальном времени. Индикация соответствия стандартам AAA/AA для черного и белого текста.
* **Fine Tuning:** Индивидуальные слайдеры (HSV) для ручной корректировки насыщенности и яркости каждого цвета в палитре.
* **Экспорт:** Сохранение результата в формате PNG с технической спецификацией.
* **Взаимодействие:** Копирование HEX-кода в буфер обмена по клику.

## 🛠 Технический стек

* **Язык:** Python 3.x
* **GUI:** Tkinter (кастомная стилизация)
* **Обработка изображений:** Pillow (PIL)
* **Логика цвета:** Colorsys, Math

## 📦 Установка и запуск

Для работы приложения требуется установленный интерпретатор Python и библиотека Pillow.

1.  **Клонируйте репозиторий или скачайте файл:**
    ```bash
    git clone <ссылка_на_репозиторий>
    ```

2.  **Установите зависимости:**
    ```bash
    pip install Pillow
    ```

3.  **Запустите приложение:**
    ```bash
    python color_gen_v5_pro.py
    ```

## 🎛 Руководство пользователя

1.  **Ввод данных:** Введите базовый HEX-код в верхнее поле (например, `#BB86FC`) или нажмите кнопку **🎲 RANDOM** для случайной базы.
2.  **Выбор режима:** Выберите алгоритм генерации в выпадающем списке (по умолчанию `🧠 Smart UI`).
3.  **Генерация:** Нажмите **⚡ GENERATE**.
4.  **Анализ:** Ознакомьтесь с показателями `On White` / `On Black` на карточках цветов. Значение `Fail` указывает на недостаточный контраст.
5.  **Калибровка:** Используйте ползунки `Sat` (Насыщенность) и `Bright` (Яркость) внутри карточки цвета для точечной настройки.
6.  **Сохранение:** Нажмите **💾 PNG** для экспорта палитры в файл.

## 👤 Автор

**fismay**